list1 = [2, 6, 4]
list2 = [2, 6, 4]

print(list1 == list2) # True. `==` checks for value equality, whether they have the same values, in the same order
print(list1 is list2) # False `is` checks for object equality, whether they are the same object, location in memory