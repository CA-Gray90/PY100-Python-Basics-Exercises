# ''.join takes exactly one argument and expects an iterable. A type error will be raised if there are any non-string values in the iterable passed as argument.
# A Type error will be raised if no arguments, or the wrong amount of arguments is provided

''.join([1, 'b'])